# Processing Manifest

The authoritative tracker of which raw sources have been processed into the wiki, and what's still queued. Refresh this whenever the backlog status is unclear — read source titles, cluster by topic, cross-reference against existing wiki pages, update the table below.

## Clusters

| Cluster | Status | Wiki Pages |
|---|---|---|
| | | |

Status values: `not started` / `in progress` / `done`.

## Processing Queue

*(nothing queued yet — items appear here as sources land in `raw/inbox/` and get triaged)*
